// src/app/utils/mathLogic.ts
export function performComplexCalculation(input: number): number {
    // Example: Calculate the square root multiplied by Pi
    // Replace this with your specific formula later
    if (input < 0) return 0;
    return Math.sqrt(input) * Math.PI;
}